A multithreaded HTTP server written in C from scratch. Implements TCP socket handling, HTTP request parsing, file serving, and concurrent connections via pthreads. Next: adding TLS for HTTPS support.
